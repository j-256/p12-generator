{
  "name": "gen-p12",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "start": "npm run build && webpack serve --config webpack.config.js",
    "screenshots": "node scripts/screenshots.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "james.klein@salesforce.com",
  "license": "Apache-2.0",
  "description": "A static website which generates p12 files in the browser for code upload to Salesforce B2C Commerce Staging instances.",
  "devDependencies": {
    "playwright": "^1.61.1",
    "webpack": "^5.99.9",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.2"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "node-forge": "^1.3.1"
  }
}
